Metadata-Version: 2.4
Name: rpa-framework
Version: 1.0.0
Summary: RPA自动化框架 - 用于快速开发自动化机器人
Author-email: RPA开发团队 <maohaidong@ygsoft.com>
Maintainer-email: RPA开发团队 <maohaidong@ygsoft.com>
License-Expression: MIT
Project-URL: Homepage, https://github.com/your-org/rpa-framework
Project-URL: Documentation, https://rpa-framework.readthedocs.io/
Project-URL: Repository, https://github.com/your-org/rpa-framework
Project-URL: Bug Reports, https://github.com/your-org/rpa-framework/issues
Keywords: rpa,automation,robot,framework,playwright,pyside6
Classifier: Development Status :: 4 - Beta
Classifier: Intended Audience :: Developers
Classifier: Operating System :: OS Independent
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Topic :: Software Development :: Libraries :: Python Modules
Classifier: Topic :: Office/Business :: Financial
Classifier: Topic :: Internet :: WWW/HTTP :: Browsers
Requires-Python: >=3.12
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: PySide6>=6.6.1
Requires-Dist: playwright==1.52.0
Requires-Dist: python-dotenv>=1.0.0
Requires-Dist: openpyxl>=3.1.0
Requires-Dist: pandas>=2.3.0
Requires-Dist: requests>=2.32.4
Provides-Extra: dev
Requires-Dist: pytest>=6.0; extra == "dev"
Requires-Dist: pytest-qt>=4.0; extra == "dev"
Requires-Dist: black>=22.0; extra == "dev"
Requires-Dist: flake8>=4.0; extra == "dev"
Requires-Dist: mypy>=0.950; extra == "dev"
Provides-Extra: docs
Requires-Dist: sphinx>=4.0; extra == "docs"
Requires-Dist: sphinx-rtd-theme>=1.0; extra == "docs"
Dynamic: license-file

# RPA自动化框架

一个基于Playwright和PySide6的RPA自动化框架，用于快速开发各种自动化机器人。

## 功能特性

- 🚀 **快速开发**: 提供完整的RPA开发框架，支持快速构建自动化机器人
- 🎯 **多种模式**: 支持GUI图形界面和CLI命令行两种运行模式
- 🔧 **灵活配置**: 支持自定义配置文件和选择器配置
- 📊 **数据处理**: 内置Excel数据处理功能
- 🎥 **录屏功能**: 支持自动化过程录屏
- 🐛 **调试模式**: 支持单步调试和错误追踪
- 📝 **日志记录**: 完整的日志记录和异常处理机制

## 安装方式

### 方式一：从离线包安装

```bash
# 安装离线包
pip install rpa-framework-1.0.0-py3-none-any.whl

# 或者从tar.gz安装
pip install rpa-framework-1.0.0.tar.gz
```

### 方式二：从源码安装

```bash
# 克隆项目
git clone <repository-url>
cd RPA-INSTALL

# 安装依赖
pip install -r requirements.txt

# 安装框架
pip install -e .
```

## 快速开始

### 1. 创建自定义Robot

创建一个简单的Robot文件 `my_robot.py`:

```python
from playwright.sync_api import sync_playwright
from rpa_framework.core.base_pw import BasePw
from rpa_framework.utils.log import logger

def main(show_browser=False, record_video=False, debug_mode=False, **kwargs):
    """
    自定义Robot主函数
    
    Args:
        show_browser: 是否显示浏览器窗口
        record_video: 是否录制视频
        debug_mode: 是否启用调试模式
        **kwargs: 其他参数
    """
    with sync_playwright() as playwright:
        with BasePw(playwright, show_browser=show_browser, record_video=record_video) as base:
            # 初始化浏览器
            base.init_browser()
            
            # 访问网页
            base.page.goto("https://example.com")
            logger.info("成功访问网页")
            
            # 执行自动化操作
            base.locate_and_click("登录按钮")
            base.locate_and_fill("用户名输入框", "your_username")
            base.locate_and_fill("密码输入框", "your_password")
            
            # 截图
            base.take_screenshot("登录页面")
            
            logger.info("Robot执行完成")
            return {
                "success": True,
                "video_file": base.get_video_path(),
                "message": "Robot执行成功"
            }

if __name__ == "__main__":
    main(show_browser=True)
```

### 2. 运行Robot

#### GUI模式运行

```bash
# 启动GUI界面
rpa-gui
```

#### CLI模式运行

```bash
# 运行自定义Robot（显示浏览器）
rpa-cli -r custom -f my_robot.py -b

# 运行自定义Robot（后台运行 + 录屏）
rpa-cli -r custom -f my_robot.py -v

# 运行内置Robot
rpa-cli -r bank_account_collector -i input.xlsx -b
```

## 核心组件

### BasePw - 基础Playwright类

提供浏览器自动化操作的核心功能：

```python
from rpa_framework.core.base_pw import BasePw
from playwright.sync_api import sync_playwright

with sync_playwright() as playwright:
    with BasePw(playwright, show_browser=True) as base:
        # 初始化浏览器
        base.init_browser()
        
        # 基本操作
        base.locate_and_click("按钮名称")      # 点击元素
        base.locate_and_fill("输入框名称", "值")  # 填充输入框
        base.locate_and_check("复选框名称")     # 勾选复选框
        base.locate_and_select("下拉框名称", "选项")  # 选择下拉框选项
        
        # 页面操作
        base.page.goto("https://example.com")  # 访问网页
        base.take_screenshot("截图名称")        # 截图
        base.page.wait_for_load_state()        # 等待页面加载
```

### 配置管理

框架支持灵活的配置管理：

```python
from rpa_framework.utils.config import config

# 获取浏览器配置
browser_config = config.get_browser_config()

# 获取超时配置
timeout_config = config.get_timeout_config()

# 获取应用配置
app_config = config.get_app_config()
```

### 日志系统

内置完整的日志记录系统：

```python
from rpa_framework.utils.log import logger

logger.info("信息日志")
logger.warning("警告日志")
logger.error("错误日志")
logger.debug("调试日志")
```

## 项目结构

```
RPA-INSTALL/
├── src/                    # 源代码目录
│   ├── core/              # 核心组件
│   │   ├── base_pw.py     # 基础Playwright类
│   │   └── page_helper.py # 页面辅助类
│   ├── robots/            # 内置Robot示例
│   │   ├── bank_account_collector.py
│   │   ├── yingdao_order_collector.py
│   │   └── shanxi_jiankong.py
│   ├── ui/                # GUI界面组件
│   │   ├── app.py         # 主应用
│   │   ├── components/    # UI组件
│   │   └── resources/     # 资源文件
│   ├── utils/             # 工具类
│   │   ├── config.py      # 配置管理
│   │   ├── log.py         # 日志系统
│   │   └── excel_utils.py # Excel工具
│   └── record/            # 录制功能
├── config/                # 配置文件
│   ├── config.json        # 主配置
│   └── selectors.json     # 元素选择器
├── data/                  # 数据文件
├── docs/                  # 文档
├── logs/                  # 日志文件
├── main.py               # GUI入口
├── main_cli.py           # CLI入口
├── setup.py              # 包配置文件
├── pyproject.toml        # 现代配置文件
├── requirements.txt      # 依赖列表
└── README.md            # 说明文档
```

## 开发指南

### 1. 创建新的Robot

1. 创建Python文件，定义main函数
2. 导入必要的模块
3. 实现自动化逻辑
4. 添加适当的日志记录和异常处理

### 2. 配置选择器

在 `config/selectors.json` 中配置元素选择器：

```json
{
  "app_name": {
    "登录按钮": "button[type='submit']",
    "用户名输入框": "input[name='username']",
    "密码输入框": "input[name='password']"
  }
}
```

### 3. 异常处理

使用框架提供的异常处理机制：

```python
from rpa_framework.utils.robot_exception import RobotsException

try:
    base.locate_and_click("按钮")
except RobotsException as e:
    logger.error(f"操作失败: {e}")
    # 处理异常
```

## 内置Robot示例

### 银行账户补采

```bash
rpa-cli -r bank_account_collector -i bank_data.xlsx -b
```

### 影刀订单采集

```bash
rpa-cli -r yingdao_order_collector -b
```

### 陕西监控

```bash
rpa-cli -r shanxi_jiankong -i monitor_data.xlsx -d -b
```

## 命令行参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `-r, --robot` | 指定要运行的robot | `-r bank_account_collector` |
| `-f, --robot-file` | 自定义robot文件路径 | `-f my_robot.py` |
| `-b, --show-browser` | 显示浏览器窗口 | `-b` |
| `-v, --record-video` | 录制屏幕视频 | `-v` |
| `-d, --debug` | 启用调试模式 | `-d` |
| `-i, --input-file` | 输入文件路径 | `-i data.xlsx` |

## 系统要求

- Python >= 3.8
- Windows 10/11 或 macOS 10.14+ 或 Linux
- 内存：建议4GB+
- 磁盘空间：建议1GB+

## 依赖包

- PySide6 >= 6.6.1 (GUI界面)
- playwright == 1.52.0 (浏览器自动化)
- python-dotenv >= 1.0.0 (环境变量管理)
- openpyxl >= 3.1.0 (Excel处理)
- pandas >= 2.3.0 (数据处理)
- requests >= 2.32.4 (HTTP请求)

## 许可证

MIT License

## 贡献指南

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 联系方式

- 邮箱: rpa@example.com
- 项目地址: https://github.com/your-org/rpa-framework
- 文档地址: https://rpa-framework.readthedocs.io/

## 常见问题

### 1. 如何安装Playwright浏览器？

```bash
# 安装框架后，运行以下命令安装浏览器
playwright install chromium
```

### 2. 如何配置环境变量？

在项目根目录创建`.env`文件：

```
PLAYWRIGHT_BROWSERS_PATH=C:\path\to\browsers
```

### 3. 如何启用调试模式？

在CLI模式下使用`-d`参数：

```bash
rpa-cli -r my_robot -d -b
```

### 4. 如何处理中文字符？

确保Python文件使用UTF-8编码，并在文件开头添加：

```python
# -*- coding: utf-8 -*-
``` 
